const anArray = [ {word : "Hello"}, {word : "World"} ];

const filtered = anArray.find((oneElement) => {
    return oneElement.word === "Hello";
});

// filtered = { word: "Hello" };

// utile pour les tableaux d'objets sinon
// utiliser anArray.indexOf();